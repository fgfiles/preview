/**
 * Created by rufian on 11/17/14.
 */
define(['game/logic/Logic',
		'orion/Application'],
function(Logic,
		 Application)
{
	var ThiefReady = (function(isNext)
	{
		var app = Application.getInstance();

//		// Setting thief's on-screen position based on Puss-in-Boots
//		var logic = app.getView(Logic.NAME);
//		var puss = logic.character;
//		var thief = logic.thief;

//		thief.setPosition(puss.position.x + (900 / 30));
	});

	return ThiefReady;
});